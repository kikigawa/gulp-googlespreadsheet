extends partials/_frame
block main
  h1 PARENT
  - var id = locals.id
  - var items = locals.parent.parent[id].lists
  - var item = Object.keys(items)

  each index, n in item
    - var id = String(index)
    - var tag = items[id].tag
    - var title = items[id].title
    - var text = items[id].text
    a(href="./#{id}" style="margin-top:50px; display:block;")
      h3 #{id}
      p #{tag}
      p #{title}
      p #{text}


  //- - var items = locals.parent
  //- - var item = Object.keys(items)
  //- - var page = getAddress(window)
  //- h3 #{test}


  //- - var i = 1
  //- while i <= 3
  //-   div(class="pickup pickup-#{i}")
  //-     each index, n in item
  //-       - var id = String(index)
  //-       - var type = items[id].type
  //-       - var title = items[id].title.replace('<br_pc>','<br>')
  //-       - var subtitle = items[id].subtitle.replace('<br_pc>','<br>')
  //-       if type == i
  //-         a(id="#{index}" href="#{items[id].url}" target="#{items[id].target}")
  //-           img(src="./images/hotoke/pickup/#{type}/#{index}.jpg")
  //-           .center
  //-             h3 !{title}
  //-             h4 !{subtitle}
  //-   - i++


